# OriginKit parking area

`originkit@0.2.23` requires an OriginKit sign-in/API key before it will return `boxcarousel` or `magneticcarousel`. The current local-only build does not request or store credentials, so no placeholder implementation is exported from this folder. Rerun the requested `originkit add` commands here after authenticating for the Day 2 components.
