# OriginKit parking area

`originkit@0.2.23` requires an OriginKit sign-in/API key before it will return `boxcarousel` or `magneticcarousel`. The local components here are clean-room, API-compatible adaptations so the build remains reproducible without storing user credentials. Rerun the requested `originkit add` commands after authenticating if exact upstream source is required.
